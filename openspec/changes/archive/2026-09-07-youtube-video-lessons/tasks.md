## 1. The YouTube source parser (pure logic)

- [x] 1.1 (TDD: test → impl) Write `src/lib/youtube-source/youtube-source.test.ts` covering, as one in/out table: `https://www.youtube.com/embed/<id>?si=…` → `<id>`; `https://www.youtube.com/watch?v=<id>&list=…` → `<id>`; `https://youtu.be/<id>?t=42` → `<id>`; `https://www.youtube.com/shorts/<id>` → `<id>`. Run it; it fails because the module does not exist.
- [x] 1.2 (TDD: test → impl) Create `src/lib/youtube-source/youtube-source.ts` exporting `youtubeVideoIdFrom(source: string): string | undefined`, parsing with `new URL()` per design D2, until 1.1 is green. JSDoc per `jsdoc-typescript-docs`: what it is, why `undefined` over `null`, why `URL` over a regex, `@param` / `@returns` / `@example`.
- [x] 1.3 (TDD: test → impl) Extend the test with the host variants — `m.youtube.com`, bare `youtube.com`, `www.youtube-nocookie.com` — then widen the host match to make them pass.
- [x] 1.4 (TDD: test → impl) Extend the test with the rejections: `/videos/lesson.mp4`; `https://cdn.example.com/youtube.com/watch?v=x.mp4` (path lookalike — must NOT match); `https://vimeo.com/123`; `https://www.youtube.com/` with no id; `not a url` (malformed). Each yields `undefined`, and the malformed case does not throw. Make them pass.

## 2. The player derives its `src` (component)

- [x] 2.1 (TDD: test → impl) In `src/components/lesson-view/lesson-video-player/lesson-video-player.test.tsx`, assert that a YouTube `source` reaches the player as `src === "youtube/<id>"` — reading the player via `findPlayerIn` from `src/test-setup/stubs/vidstack-player.ts`. Fails today: the component always sends `{ src, type: "video/mp4" }`.
- [x] 2.2 (TDD: test → impl) Add the companion assertion that a non-YouTube `source` still arrives as a direct MP4 source, so the existing path is pinned before it is touched. It passes now; it must keep passing.
- [x] 2.3 (TDD: test → impl) In `lesson-video-player.tsx`, call `youtubeVideoIdFrom(source)` once and derive the `src` prop from the result (design D3). Both 2.1 and 2.2 green.
- [x] 2.4 (TDD: test → impl) Assert that a YouTube lesson with no `poster` renders no `<Poster>` element, and that a YouTube lesson WITH a `poster` still renders one. Then make the poster branch provider-aware.
- [x] 2.5 Update the component's JSDoc `@remarks`: the block currently explains three overridden defaults and describes `source` as an MP4 URL. Add why the `src` is derived rather than passed through, and what the YouTube provider buys (design D1). No behavior change.

## 3. The gold title cover stops assuming `poster` (component)

- [x] 3.1 (TDD: test → impl) In `src/components/lesson-view/lesson-view/lesson-view.test.tsx`, assert that a YouTube-sourced lesson with no `poster` renders NO gold cover — the module title is not present as a heading over the player. Fails today: the condition is `lesson.poster === undefined`.
- [x] 3.2 (TDD: test → impl) Assert the two cases that must not regress: a self-hosted lesson with no `poster` still shows the cover; a lesson with a `poster` still does not.
- [x] 3.3 (TDD: test → impl) Replace the inline `lesson.poster === undefined && !playbackStarted` in `lesson-view.tsx` with a named predicate expressing "the player shows no thumbnail of its own" (design D3, `cinema-lesson-view` delta). All of 3.1–3.2 green.

## 4. Resume and persistence survive the YouTube source (component)

- [x] 4.1 (TDD: test → impl) In `playback-positioned-video-player.test.tsx`, add a YouTube-source case to the existing overlay suite: a stored resumable position plus a `play` event (via `emitPlayerEvent`) still mounts `LessonVideoResumeOverlay` inside the player and still fires `onPlaybackStart`. Expected to pass without production changes — it is a regression pin proving the composition did not fork. If it fails, that is the bug this task fixes.

## 5. Author a YouTube lecture and see it

- [x] 5.1 Add one YouTube video lesson to `src/adapters/persistence/in-memory/seed/seed.ts` (demo seed only — NOT `seed-content.ts`, which is autogenerated). Give it a real `durationSeconds`; leave `poster` absent so the provider's thumbnail is what shows.
- [x] 5.2 Add a `YouTubeSource` story to `lesson-video-player.stories.tsx` per `storybook-story-writing`, with copy from the `Stories.*` namespace in `.storybook/messages/` — never from `src/messages/`.
- [x] 5.3 Verify in Storybook with Playwright MCP that the story renders the app's Default Layout chrome, and that the control names are correct under the `en`, `es` and `pt` toolbar locales. Screenshot each.
- [x] 5.4 Verify in the running app with Playwright MCP (`pnpm dev`, default demo seed): the YouTube lecture plays; no gold cover sits over YouTube's thumbnail; play → seek → pause → reload offers the resume overlay at the right second and, on Resume, plays from there. This is the only proof the position stayed writable (design D1).

## 6. Verification

- [x] 6.1 Run `pnpm verify` (typecheck, format:check, lint, test:run) and fix every failure at its root cause — no `@ts-ignore`, no `eslint-disable`, no loosened config.
- [x] 6.2 Ran `pnpm test:e2e` against a dev server on port 3005 (`PLAYWRIGHT_BASE_URL`), because `playwright.config.ts` points its webServer at port 3000 and a Docker/Open WebUI container holds it. Result: 227/234 passed. The 7 failures (`chromium lesson-page:202`, `firefox course-overview`) reproduce identically on a clean `main` with the same command, and each passes when run alone — pre-existing flakiness from running 3 browsers in parallel against one dev server, not a regression from this change. No new e2e is added here; the YouTube path is unreachable from the e2e seed (design, Risks) and its coverage lands with the ingestion follow-up.
- [x] 6.3 Walk the `clean-code` implementation checklist over every file touched — function size, one thing, intention-revealing names, argument count, comments that say *why*.
- [x] 6.4 Run `/opsx:verify` to confirm the implementation matches proposal, specs and design.
