## 1. Player reports the first play

- [x] 1.1 (TDD: test → impl) In `src/components/lesson-view/playback-positioned-video-player/playback-positioned-video-player.test.tsx`, add a failing case asserting that an `onPlaybackStart` prop is invoked when the `<video>` emits `play`, plus a case asserting no throw when the prop is omitted. Mirror the existing media-event driving pattern in that file.
- [x] 1.2 (TDD: impl after 1.1) Add the optional `onPlaybackStart?: () => void` prop to `PlaybackPositionedVideoPlayer` and call it from the existing `onPlay` listener (the same one that flips `hasInteractedRef`), holding it in a ref rather than in the effect's dependency list so an inline caller arrow cannot re-subscribe the listeners each render (see design.md D2). Do not add a second `play` subscription.

## 2. Cover renders only when it is a cover

- [x] 2.1 (TDD: test → impl) In `src/components/lesson-view/lesson-view/lesson-view.test.tsx`, rewrite the existing test `"video lesson renders a cinema hero overlay (module title) while preserving the native player"` into the poster-less idle case: the module title IS present as a heading, a `<video>` exists, and `[aria-current="page"]` is set in the outline. Confirm it still passes before moving on.
- [x] 2.2 (TDD: test → impl) Add a failing case: a lesson view whose video lesson carries a `poster` renders NO module-title heading over the player (`queryByRole("heading", { name: "Module" })` is null). Extend the local `fixtures()` helper with a poster option rather than duplicating the fixture.
- [x] 2.3 (TDD: test → impl) Add a failing case: for a poster-less lesson, firing `play` on the rendered `<video>` (inside `act`) removes the module-title heading. This is the regression guard for the reported bug.
- [x] 2.4 (TDD: impl after 2.2–2.3) In `src/components/lesson-view/lesson-view/lesson-view.tsx`, add `const [playbackStarted, setPlaybackStarted] = useState(false)`, pass `onPlaybackStart={() => setPlaybackStarted(true)}` to `PlaybackPositionedVideoPlayer`, and gate the overlay block on `!lesson.poster && !playbackStarted`. Keep the overlay markup, the gradient scrim, the `Eyebrow` import, and the container classes exactly as they are. The latch is one-way — never reset it on pause, seek, or end.

## 3. Verification

- [x] 3.1 (TDD: verification, no new code) Run `pnpm test:run` — the full unit/component suite must pass, including the untouched `native-video-player` and `lesson-video-resume` tests.
- [x] 3.2 (TDD: verification, no new code) Run `pnpm test:e2e` for the lesson specs (`e2e/lesson-page.spec.ts`, `e2e/lesson-playback-resume.spec.ts`) unchanged, confirming playback and resume behavior are unaffected. **Result: 10 failures, all pre-existing.** Baselined by stashing this change and re-running: baseline fails 11 (4 in `lesson-page`, 7 in `lesson-playback-resume`), this change fails 10 — the same tests, all `Test timeout of 30000ms exceeded` / teardown timeouts, none assertion failures. No regression from this change; the branch's e2e timeouts are a separate pre-existing problem.
- [x] 3.3 (TDD: verification, no new code) Drove the real app (`USE_COURSE_CONTENT_SEED=1`) at the very lesson from the bug report (`Fast /i/`) with a headless Playwright script, reporting cover state at idle / playing / paused. **Result: `coverVisible: false` in all three states.** Note: every one of the 107 seeded video lessons carries a `poster`, so in the real app the cover never renders at all — the poster-less branch is exercised by the unit tests. The white "FAST /i/" still visible mid-frame is burned into the source video's own intro card, not application chrome.
