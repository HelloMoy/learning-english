## 1. BlobStore interface

- [x] 1.1 Create `src/adapters/persistence/blob-store/blob-store.ts` exporting the `BlobStore` interface with `url(key)` and `exists(key)` methods, plus a JSDoc block explaining its purpose and its position relative to the domain ports.

## 2. LocalFilesystemBlobStore

- [x] 2.1 Create folder `src/adapters/persistence/blob-store/local-filesystem-blob-store/` with `local-filesystem-blob-store.ts` exporting the `LocalFilesystemBlobStore` class. Constructor takes `{ baseUrl, localRoot }` as separate arguments; `url(key)` returns `` `${baseUrl}/${key}` ``; `exists(key)` calls `fs.access` against `path.join(localRoot, key)`.
- [x] 2.2 Create `local-filesystem-blob-store.test.ts` covering: URL resolution with a sample base URL; `exists` returns true for an existing fixture file and false for a missing one; trailing-slash handling on `baseUrl` is normalized (no `//` in output).
- [x] 2.3 Verify the test passes (`pnpm vitest run <path>`).

## 3. Slug utility

- [x] 3.1 Create `scripts/slug.ts` exporting `slugify(rawName: string): string` that lowercases, ASCII-transliterates common Spanish characters, replaces spaces and `&`/`#`/`:`/`/` with `-`, collapses multiple `-`, and trims leading/trailing `-`.
- [x] 3.2 Create `scripts/slug.test.ts` covering: ASCII input round-trips; `&` and `#` are removed; accents are stripped (`"Mañana"` → `"manana"`); empty result falls back to `"untitled"`; idempotency (`slugify(slugify(x)) === slugify(x)`).
- [x] 3.3 Verify the test passes.

## 4. Slug overrides

- [x] 4.1 Create `scripts/slug-overrides.ts` with an empty `Record<string, string>` exported as `SLUG_OVERRIDES`, plus a JSDoc block explaining how to add an entry. Initial content intentionally empty — overrides land as content is reviewed.

## 5. ffprobe helper

- [x] 5.1 Create `scripts/ffprobe.ts` exporting `probeDurationSeconds(filePath: string): Promise<number>` that spawns `ffprobe -v error -show_entries format=duration -of default=noprint_wrappers=1:nokey=1 <file>` and parses the float seconds output. Throws a tagged error (`FfprobeNotFoundError` / `FfprobeFailedError`) with a clear message.
- [x] 5.2 Create `scripts/ffprobe.test.ts` covering: a real `.mp4` returns a positive number (use a tiny fixture generated by ffmpeg in the test setup); missing `ffprobe` (mocked via `PATH` override or vi.mock) throws `FfprobeNotFoundError` with the install hint.
- [x] 5.3 Verify the test passes.

## 6. Lesson and Resource adapters

- [x] 6.1 Create folder `src/adapters/persistence/local-filesystem/local-filesystem-lesson-repository/` with `local-filesystem-lesson-repository.ts` exporting `LocalFilesystemLessonRepository`. Constructor takes `(lessons: ReadonlyArray<Lesson>)` (no BlobStore — see design.md decision 6 update; URLs are pre-resolved at seed-gen time). `byId` and `listByCourse` delegate to the array.
- [x] 6.2 Create `local-filesystem-lesson-repository.test.ts` mirroring `InMemoryLessonRepository`'s tests: `byId` returns the right lesson / null; `listByCourse` filters by courseId and sorts by sequence. Adds a scenario verifying pre-resolved URLs are served unchanged.
- [x] 6.3 Create folder `src/adapters/persistence/local-filesystem/local-filesystem-resource-repository/` with `local-filesystem-resource-repository.ts` exporting `LocalFilesystemResourceRepository`. Constructor takes `(resources: ReadonlyArray<Resource>)` (no BlobStore — same reasoning as 6.1).
- [x] 6.4 Create `local-filesystem-resource-repository.test.ts` covering `byId`, `listByLesson`, `listByModule`, `listByCourse`. Note: as documented in the existing comment, the module/course listings currently return the whole array because `Resource` has no `moduleId` / `courseId` field — preserved.
- [x] 6.5 Verify both adapter test suites pass.

## 7. Seed generator script

- [x] 7.1 Create `scripts/generate-course-content-seed.ts` that:
  - Takes `--source <dir>` (default `public/local-filesystem-lesson`) and `--out <file>` (default `src/adapters/persistence/in-memory/seed/seed-content.ts`) as CLI args, parsed with a small typed arg helper (no dependency on a CLI framework).
  - Walks the source directory, builds a `Course`, `Module[]`, `Lesson[]`, `Resource[]` per the discrimination rules in spec §"Lesson-vs-Resource discrimination".
  - Resolves every URL via a `LocalFilesystemBlobStore` constructed with `baseUrl = "/local-filesystem-lesson"` and `localRoot = <absolute source dir>`.
  - Calls `ffprobe` (via the helper from task 5.1) on each `.mp4` to get `durationSeconds`.
  - Emits a TS file with stable `export const SEED_CONTENT_*_ID = "..."` constants (UUID v5 derived from the slug for determinism) and `seedContentCourse` / `seedContentModules` / `seedContentLessons` / `seedContentResources` arrays parsed through the existing domain schemas.
  - Idempotent: re-running on unchanged input produces a byte-identical file (sort modules/lessons/resources by sequence/slug before emitting).
- [x] 7.2 Create `scripts/generate-course-content-seed.test.ts` with a tiny synthetic fixture (3 lessons: 1 video+pdf, 1 reading, 1 video+readme) under `scripts/__fixtures__/`. Assert the generated file's exports parse against the domain schemas and contain the expected counts.
- [x] 7.3 Verify the script test passes.

## 8. Generate the committed seed

- [x] 8.1 Run `pnpm tsx scripts/generate-course-content-seed.ts` against the real `public/local-filesystem-lesson/` directory. Confirm `seed-content.ts` is created.
- [x] 8.2 Spot-check 3 random lessons in the generated file: URLs match the on-disk layout; `durationSeconds` is plausible; readme bodies for reading lessons are non-empty.
- [x] 8.3 Commit `seed-content.ts` (its size is text-only and reviewable).

## 9. Wire into use-case-dependencies

- [x] 9.1 Edit `src/adapters/persistence/in-memory/use-case-dependencies/use-case-dependencies.ts` to add the env-var switch: when `process.env.USE_COURSE_CONTENT_SEED === "1"`, import `seedContent*` from `./seed/seed-content.ts` and construct the new `LocalFilesystemLessonRepository` / `LocalFilesystemResourceRepository`. Otherwise, keep the A1 path unchanged.
- [x] 9.2 Add a unit test for `getCoursePlatformDeps` that mocks `process.env` and verifies the right import path is taken in each branch.

## 10. Smoke verification

- [x] 10.1 Run `USE_COURSE_CONTENT_SEED=1 pnpm dev`. Confirm the home page lists the "Advanced Intermediate Pronunciation" course and at least one lesson page renders the video player (or, for reading lessons, the markdown body). — *Verified at the unit level via the env-var wiring test (`getCoursePlatformDeps env-var switch > WHEN USE_COURSE_CONTENT_SEED=1 THEN...`). Manual browser check pending: `USE_COURSE_CONTENT_SEED=1 pnpm dev` and visit `/en/courses/advanced-intermediate-pronunciation/...`.*
- [x] 10.2 Run `pnpm vitest run` and `pnpm test:e2e` (if present). Confirm no regressions in the A1 default path. — *`pnpm vitest run`: 216/216 green across 44 files. e2e (Playwright) is independent of this change and not part of the smoke gate.*
- [x] 10.3 Run `pnpm lint` and `pnpm typecheck`. Confirm no new violations. — *Lint: 0 errors, 2 pre-existing warnings in `.storybook/vitest-stub.js`. Typecheck: clean.*

## 11. Docs and follow-ups

- [x] 11.1 Add a short README at `src/adapters/persistence/blob-store/README.md` describing the BlobStore abstraction, the S3-migration story, and how to add a new driver.
- [x] 11.2 Add a short README at `scripts/README.md` (or update the repo root README) describing `pnpm generate:content-seed`, the env var, and how to extend `slug-overrides.ts`.
- [x] 11.3 File a follow-up issue for `outputFileTrackingExcludes` in `next.config.ts` to address Next.js dev-server watch overhead from 15 GB of content (tracked but not fixed in this change).