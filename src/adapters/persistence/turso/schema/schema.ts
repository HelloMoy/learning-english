/**
 * Every table the application stores, in one module, so Drizzle Kit generates
 * migrations for all of them and the Drizzle client knows every relation.
 *
 * @category Persistence
 */
export * from "./auth-schema";
export * from "./learner-schema";
